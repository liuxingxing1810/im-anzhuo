# Aurora Wave - 项目主文档

> 📅 更新日期：2025年12月10日  
> 📱 版本：0.1.3-dev

---

## 📋 目录

1. [项目概述](#1-项目概述)
2. [技术架构](#2-技术架构)
3. [模块说明](#3-模块说明)
4. [页面结构](#4-页面结构)
5. [UI 设计风格](#5-ui-设计风格)
6. [核心功能](#6-核心功能)
7. [数据流设计](#7-数据流设计)
8. [导航结构](#8-导航结构)
9. [当前状态与待办](#9-当前状态与待办)
10. [问题清单](#10-问题清单)
11. [开发指南](#11-开发指南)
12. [构建与运行](#12-构建与运行)

---

## 1. 项目概述

### 1.1 产品定位
Aurora Wave（极光波）是一款现代化的即时通讯应用，类似微信，提供消息聊天、联系人管理、社交发现等功能。

### 1.2 目标用户
- 普通社交用户
- 需要即时通讯功能的团队

### 1.3 核心特性
- 💬 即时消息（文字、图片、语音、文件、位置）
- 👥 联系人与好友管理
- 🔍 发现页（朋友圈、扫一扫、附近的人等）
- 👤 个人中心与设置
- 🌙 深色模式支持
- 🌐 中英文双语

---

## 2. 技术架构

### 2.1 技术栈

| 层级 | 技术选型 |
|------|----------|
| **语言** | Kotlin 1.9.x |
| **UI 框架** | Jetpack Compose + Material 3 |
| **架构模式** | MVVM + Clean Architecture |
| **依赖注入** | Hilt (Dagger) |
| **本地存储** | Room Database |
| **网络请求** | Retrofit + OkHttp + Kotlin Serialization |
| **异步处理** | Kotlin Coroutines + Flow |
| **图片加载** | Coil |
| **日志** | Timber |
| **导航** | Navigation Compose |

### 2.2 架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        App Module                            │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              MainActivity + Navigation               │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        ▼                     ▼                     ▼
┌───────────────┐   ┌───────────────┐   ┌───────────────┐
│   Feature:    │   │   Feature:    │   │   Feature:    │
│   Messages    │   │  Connections  │   │   Discover    │
├───────────────┤   ├───────────────┤   ├───────────────┤
│ • RootScreen  │   │ • RootScreen  │   │ • RootScreen  │
│ • ChatDetail  │   │ • ContactDetail│  │ • MomentsScreen│
│ • ViewModel   │   │ • ViewModel   │   │ • ScanScreen  │
│ • UI Components│  │ • UI Components│  │ • MyQrCode    │
└───────────────┘   └───────────────┘   └───────────────┘
        │                     │                     │
        └─────────────────────┼─────────────────────┘
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                       Core Modules                           │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐  ┌────────┐│
│  │   :data    │  │  :network  │  │  :design   │  │:common ││
│  ├────────────┤  ├────────────┤  ├────────────┤  ├────────┤│
│  │ Repository │  │ ApiConfig  │  │ AuroraTheme│  │Utilities││
│  │ Room DAO   │  │ Retrofit   │  │ Colors     │  │Dispatcher│
│  │ Entities   │  │ OkHttp     │  │ Typography │  │         ││
│  └────────────┘  └────────────┘  └────────────┘  └────────┘│
└─────────────────────────────────────────────────────────────┘
```

### 2.3 包结构

```
com.aurora.wave/
├── app/                    # 主应用模块
│   ├── MainActivity.kt     # 入口 + 导航
│   └── WaveApp.kt          # Application (Hilt)
├── core/
│   ├── common/             # 通用工具
│   ├── data/               # 数据层
│   │   ├── dao/            # Room DAO
│   │   ├── entity/         # 数据库实体
│   │   ├── model/          # 业务模型
│   │   ├── repository/     # Repository 接口与实现
│   │   └── service/        # IM 服务接口
│   ├── design/             # 设计系统
│   │   └── AuroraTheme.kt  # 主题配置
│   └── network/            # 网络层
│       └── ApiConfig.kt    # Retrofit 配置
└── feature/
    ├── messages/           # 消息功能
    ├── connections/        # 联系人功能
    ├── discover/           # 发现页功能
    ├── profile/            # 个人中心
    └── settings/           # 设置页
```

---

## 3. 模块说明

### 3.1 App 模块 (`:app`)
- **职责**：应用入口、导航配置、Hilt 初始化
- **关键文件**：
  - `MainActivity.kt` - Compose 导航宿主
  - `WaveApp.kt` - Hilt Application

### 3.2 Core 模块

| 模块 | 职责 |
|------|------|
| `:core:data` | 数据持久化、Repository 模式、Room 数据库 |
| `:core:network` | 网络请求配置、API 接口定义 |
| `:core:design` | 主题、颜色、排版、通用 UI 组件 |
| `:core:common` | 工具类、扩展函数、Dispatcher |

### 3.3 Feature 模块

| 模块 | 包含屏幕 | 状态 |
|------|----------|------|
| `:feature:auth` | 登录页、注册页 | ✅ UI 完成 |
| `:feature:messages` | 消息列表、聊天详情 | ✅ UI 完成 |
| `:feature:connections` | 联系人列表、联系人详情、好友请求 | ✅ UI 完成 |
| `:feature:discover` | 发现首页、朋友圈、扫一扫、我的二维码 | ✅ UI 完成 |
| `:feature:profile` | 个人中心、退出登录 | ✅ UI 完成 |
| `:feature:settings` | 设置详情页 | ✅ UI 完成 |

---

## 4. 页面结构

### 4.1 认证流程

```
┌─────────────┐     注册     ┌─────────────┐
│  登录页面    │◀───────────▶│  注册页面    │
│ LoginScreen │              │RegisterScreen│
└─────────────┘              └─────────────┘
       │
       │ 登录成功
       ▼
┌─────────────────────────────────────────┐
│            主页 (4个Tab)                 │
└─────────────────────────────────────────┘
```

### 4.2 主页面结构

```
┌─────────────────────────────────────────────────────────────┐
│                       主应用 (MainActivity)                  │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐       │
│  │  消息    │  │ 联系人  │  │  发现   │  │   我    │       │
│  │Messages │  │Contacts │  │Discover │  │Profile  │       │
│  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘       │
│       │            │            │            │             │
│       ▼            ▼            ▼            ▼             │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐       │
│  │消息列表 │  │联系人列表│  │发现菜单 │  │个人信息 │       │
│  │         │  │         │  │         │  │         │       │
│  │ · 搜索  │  │ · 好友  │  │ · 扫一扫│  │ · 头像  │       │
│  │ · 会话  │  │ · 分组  │  │ · 我的  │  │ · Wave ID│      │
│  │         │  │ · 索引  │  │   二维码│  │ · 退出  │       │
│  └────┬────┘  └────┬────┘  └────┬────┘  └─────────┘       │
│       │            │            │                          │
│       ▼            ▼            ▼                          │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐                    │
│  │聊天详情 │  │联系人详情│  │朋友圈   │                    │
│  │ChatDetail│ │ContactDetail│MomentsScreen│               │
│  └─────────┘  └─────────┘  └─────────┘                    │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 4.3 各页面详情

| 页面 | 路由 | 文件位置 | 功能说明 |
|------|------|----------|----------|
| **登录页** | `login` | `feature/auth/LoginScreen.kt` | 账号密码登录，动态波浪背景 |
| **注册页** | `register` | `feature/auth/RegisterScreen.kt` | 账号注册 + 邀请码 |
| **消息列表** | `messages` | `feature/messages/MessagesRootScreen.kt` | 会话列表，显示最新消息和时间 |
| **聊天详情** | `chat/{id}` | `feature/messages/ChatDetailScreen.kt` | 消息详情，支持多种消息类型 |
| **联系人列表** | `connections` | `feature/connections/ConnectionsRootScreen.kt` | 按字母分组，侧边索引 |
| **联系人详情** | `contact/{id}` | `feature/connections/ContactDetailScreen.kt` | 联系人信息，发消息/语音/视频 |
| **发现页** | `discover` | `feature/discover/DiscoverRootScreen.kt` | 功能入口列表 |
| **朋友圈** | `moments` | `feature/discover/MomentsScreen.kt` | 朋友动态流 |
| **扫一扫** | `scan` | `feature/discover/ScanScreen.kt` | 二维码扫描 |
| **我的二维码** | `my_qr_code` | `feature/discover/MyQrCodeScreen.kt` | 个人二维码展示 |
| **个人中心** | `profile` | `feature/profile/ProfileRootScreen.kt` | 个人信息 + 退出登录 |
| **设置** | `settings` | `feature/settings/SettingsScreen.kt` | 应用设置（已隐藏入口） |

---

## 5. UI 设计风格

### 5.1 整体风格
- **设计语言**：Material 3 (Material You)
- **参考风格**：微信 + 现代 IM 应用（Telegram/Discord）
- **主色调**：绿色系（类似微信）
- **深色模式**：完全支持

### 5.2 配色方案

| 用途 | 浅色模式 | 深色模式 |
|------|----------|----------|
| 主色 (Primary) | `#07C160` 绿色 | `#07C160` |
| 背景 (Background) | `#EDEDED` | `#111111` |
| 卡片 (Surface) | `#FFFFFF` | `#1E1E1E` |
| 文字 (OnSurface) | `#191919` | `#E5E5E5` |
| 次要文字 | `#999999` | `#666666` |

### 5.3 设计特点

#### 登录/注册页
- 现代简洁风格，无传统 Logo
- **动态背景**：流动的波浪线条 + 渐隐渐现的圆形装饰
- 填充式输入框（无边框，圆角 14dp）
- 大圆角按钮（16dp）
- 入场动画：淡入 + 上滑

#### 消息列表
- 会话卡片：白色/深色背景
- 头像圆角：8dp
- 未读消息红点显示
- 搜索框位于顶部

#### 联系人
- 按字母分组
- 侧边字母索引
- Tab 切换（好友/新朋友）

#### 个人中心
- 头像居中对齐
- Wave ID 展示
- 退出登录按钮（红色文字）

### 5.4 组件规范

| 组件 | 规范 |
|------|------|
| 圆角 | 小: 8dp, 中: 12dp, 大: 16dp |
| 间距 | 8dp / 12dp / 16dp / 24dp |
| 字体大小 | 标题: 18sp, 正文: 14sp, 副标题: 12sp |
| 按钮高度 | 48dp - 54dp |
| 头像大小 | 小: 40dp, 中: 56dp, 大: 80dp |

---

## 6. 核心功能

### 4.1 消息模块
```
消息列表 ──点击──▶ 聊天详情
    │                  │
    │                  ├── 文字消息
    │                  ├── 图片消息
    │                  ├── 语音消息
    │                  ├── 文件消息
    │                  └── 位置消息
    │
    └── 搜索过滤
```

**支持的消息类型**：
- `TEXT` - 文字消息
- `IMAGE` - 图片消息
- `VOICE` - 语音消息
- `VIDEO` - 视频消息
- `FILE` - 文件消息
- `LOCATION` - 位置消息
- `SYSTEM` - 系统消息

### 4.2 联系人模块
- 联系人按字母分组
- 字母索引快速定位
- 好友请求列表（New Friends Tab）
- 联系人详情页（发消息/语音通话/视频通话）

### 4.3 发现模块

| 功能 | 路由 | 状态 |
|------|------|------|
| 朋友圈 | `moments` | ✅ UI 完成 |
| 扫一扫 | `scan` | ✅ UI 完成 |
| 附近的人 | `nearby` | 🔲 待实现 |
| 探索 | `explore` | 🔲 待实现 |
| 小程序 | `mini_apps` | 🔲 待实现 |
| 游戏 | `games` | 🔲 待实现 |
| 购物 | `shop` | 🔲 待实现 |
| 热点资讯 | `news` | 🔲 待实现 |

### 4.4 个人中心
- 用户头像与基本信息
- Wave ID 展示
- 服务入口（支付/卡包/乘车）
- 收藏/设置/通知/隐私/存储/外观/帮助/关于

---

## 7. 数据流设计

### 5.1 MVVM 数据流

```
┌─────────┐    observe     ┌───────────┐    call     ┌────────────┐
│   UI    │◀──────────────│ ViewModel │────────────▶│ Repository │
│ (Screen)│                │  (State)  │             │            │
└─────────┘                └───────────┘             └────────────┘
     │                           │                         │
     │ user action               │ update state            │
     ▼                           │                         ▼
┌─────────┐                      │                 ┌────────────┐
│ onClick │──────────────────────┘                 │ Room / API │
└─────────┘                                        └────────────┘
```

### 5.2 State 定义示例

```kotlin
data class ConversationListState(
    val conversations: List<ConversationUiModel> = emptyList(),
    val searchQuery: String = "",
    val isLoading: Boolean = false,
    val error: String? = null
)
```

### 5.3 Repository 模式

```kotlin
interface MessageRepository {
    fun getMessagesForConversation(conversationId: String): Flow<List<MessageEntity>>
    suspend fun sendTextMessage(conversationId: String, text: String): Result<MessageEntity>
    suspend fun markAllMessagesAsRead(conversationId: String): Result<Unit>
    // ...
}
```

---

## 8. 导航结构

### 6.1 底部导航

| Tab | 路由 | 图标 |
|-----|------|------|
| 消息 | `messages` | Chat |
| 联系人 | `connections` | Contacts |
| 发现 | `discover` | Compass |
| 我 | `profile` | AccountCircle |

### 8.2 完整路由表

```kotlin
// 认证页面
"login"         → LoginScreen      // 登录
"register"      → RegisterScreen   // 注册

// 主 Tab
"messages"      → MessagesRootScreen
"connections"   → ConnectionsRootScreen
"discover"      → DiscoverRootScreen
"profile"       → ProfileRootScreen

// 二级页面
"chat/{conversationId}"   → ChatDetailScreen
"contact/{contactId}"     → ContactDetailScreen
"moments"                 → MomentsScreen
"scan"                    → ScanScreen
"my_qr_code"              → MyQrCodeScreen
"settings"                → SettingsScreen (入口已隐藏)
"about"                   → AboutScreen
```

---

## 9. 当前状态与待办

### 9.1 已完成 ✅
- [x] 多模块项目架构搭建
- [x] Hilt 依赖注入配置
- [x] Material 3 主题系统
- [x] 底部导航 + Navigation Compose
- [x] 消息列表与聊天 UI
- [x] 联系人列表与详情 UI
- [x] 发现页 UI（朋友圈/扫一扫/我的二维码）
- [x] 个人中心与设置 UI
- [x] 中英文双语支持
- [x] Room 数据库 Entity/DAO 定义
- [x] Repository 接口定义
- [x] **登录页面** - 现代风格 + 动态波浪背景
- [x] **注册页面** - 表单验证 + 邀请码
- [x] **用户数据持久化** - UserDataStore (DataStore)
- [x] **登录状态管理** - 启动检查 + 退出登录
- [x] **退出登录功能** - 清除缓存返回登录页
- [x] 隐藏设置入口（从个人中心移除）
- [x] 隐藏发现页部分菜单项（朋友圈/视频号/表情）
- [x] 隐藏个人中心状态按钮
- [x] **语言切换功能** - 完整实现中英文切换（即时生效）
- [x] **locales_config.xml** - Android 13+ 按应用语言设置支持
- [x] **LanguageManager** - 语言管理器（DataStore 持久化 + Activity 重启）
- [x] 移除消息页右下角 FAB 浮动按钮

### 9.2 待完成 🔲

#### 高优先级
- [ ] **真实登录/注册 API** 对接后端
- [ ] ViewModel Hilt 注入改造
- [ ] Repository 真实实现
- [ ] ImService 消息服务对接
- [ ] 消息推送（FCM/WebSocket）

#### 中优先级
- [ ] 深色模式全局状态管理
- [ ] 图片消息发送功能
- [ ] 语音消息发送功能
- [ ] 扫码功能（CameraX 集成）
- [ ] 位置功能（Google Maps）
- [ ] 联系人搜索功能

#### 低优先级 (发现页扩展)
- [ ] 附近的人
- [ ] 探索
- [ ] 小程序
- [ ] 游戏
- [ ] 购物
- [ ] 热点资讯

---

## 10. 问题清单

### 8.1 P0 严重问题
> 当前无 P0 问题

### 8.2 P1 重要问题

| # | 问题 | 影响 | 修复建议 |
|---|------|------|----------|
| 1 | **数据层全为 Mock** | 无法真实聊天 | 完成 Repository + ImService 对接 |
| 2 | **ViewModel 未使用 Hilt** | DI 不完整，测试困难 | 添加 `@HiltViewModel` |
| 3 | **缺少 INTERNET 权限** | 联网会崩溃 | AndroidManifest 添加权限 |
| 4 | **消息发送无网络请求** | 消息不会送达 | 调用 Repository 发送 |
| 5 | **Dark Mode 开关无效** | 切换主题无反应 | 用全局 ThemeState 管理 |

### 8.3 P2 一般问题

| # | 问题 | 修复建议 |
|---|------|----------|
| 1 | 搜索每次重建列表 | 用 StateFlow + combine 过滤 |
| 2 | DateHeader 硬编码 "Today" | 动态计算日期 |
| 3 | ProfileRootScreen remember 参数过多 | 抽成 data class |
| 4 | 无日志记录 | 关键路径加 Timber |
| 5 | Release 未启用 minify | 开启 R8 混淆 |

### 8.4 P3 代码风格

| # | 问题 | 修复建议 |
|---|------|----------|
| 1 | 部分 Composable 超 50 行 | 拆分子组件 |
| 2 | 部分字符串未国际化 | 抽到 strings.xml |
| 3 | getIconBackgroundColor 圈复杂度高 | 改用 Map |
| 4 | 路由用魔法字符串 | 用 sealed class |

---

## 11. 开发指南

### 9.1 添加新功能模块

1. 在 `feature/` 下创建新模块目录
2. 添加 `build.gradle.kts`
3. 在 `settings.gradle.kts` 中 include
4. 在 `app/build.gradle.kts` 中添加依赖
5. 创建 Screen + ViewModel
6. 在 `MainActivity.kt` 添加路由

### 9.2 添加新 API 接口

```kotlin
// 1. 在 core:network 定义接口
interface UserApi {
    @GET("users/{id}")
    suspend fun getUser(@Path("id") id: String): UserResponse
}

// 2. 在 core:data 创建 Repository
class UserRepositoryImpl(private val api: UserApi) : UserRepository {
    override suspend fun getUser(id: String) = api.getUser(id)
}

// 3. 在 Hilt Module 提供依赖
@Provides fun provideUserApi(retrofit: Retrofit) = retrofit.create(UserApi::class.java)
```

### 9.3 代码规范

- Composable 函数不超过 50 行
- ViewModel 必须使用 `@HiltViewModel`
- 所有用户可见字符串放 `strings.xml`
- 关键操作添加 `Timber.d()` 日志
- 异常必须捕获并返回 `Result.failure()`

---

## 12. 构建与运行

### 10.1 环境要求

| 工具 | 版本 |
|------|------|
| Android Studio | Hedgehog+ |
| JDK | 17+ |
| Gradle | 8.x |
| minSdk | 24 |
| targetSdk | 34 |
| compileSdk | 34 |

### 10.2 构建命令

```bash
# 编译 Debug APK
./gradlew :app:assembleDebug

# 安装到设备
./gradlew :app:installDebug

# 运行单元测试
./gradlew test

# 检查代码
./gradlew lint
```

### 10.3 模拟器测试

#### 创建模拟器（ARM64 for Apple Silicon）

```bash
# 安装 ARM64 系统镜像
yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "system-images;android-30;google_apis;arm64-v8a"

# 创建模拟器
echo "no" | $ANDROID_HOME/cmdline-tools/latest/bin/avdmanager create avd \
  -n Test_API30 \
  -k "system-images;android-30;google_apis;arm64-v8a" \
  -d "pixel_4" \
  --force

# 启动模拟器
$ANDROID_HOME/emulator/emulator -avd Test_API30 -no-snapshot -gpu host -memory 2048 -no-audio &

# 等待设备启动完成
adb wait-for-device shell getprop sys.boot_completed
```

#### 安装与启动应用

```bash
# 构建并安装
./gradlew :app:assembleDebug
adb install -r app/build/outputs/apk/debug/app-debug.apk

# 启动应用
adb shell am start -n com.aurora.wave.dev/com.aurora.wave.MainActivity
```

#### UI Automator 自动化测试（AI 使用）

UI Automator 是 Android SDK 自带的 UI 自动化工具，无需额外安装。

```bash
# 获取当前界面 UI 结构（XML 格式）
adb shell uiautomator dump /sdcard/ui.xml
adb pull /sdcard/ui.xml /tmp/ui.xml

# 解析元素（获取文本和坐标）
grep -oE '(text|bounds)="[^"]*"' /tmp/ui.xml

# 根据坐标精确点击元素
adb shell input tap <x> <y>

# 输入文字
adb shell input text "hello"

# 模拟返回键
adb shell input keyevent KEYCODE_BACK

# 模拟 Home 键
adb shell input keyevent KEYCODE_HOME
```

**UI dump XML 结构说明**：
- `text` - 元素显示的文本
- `bounds="[x1,y1][x2,y2]"` - 元素的屏幕坐标区域
- `clickable="true"` - 元素是否可点击
- `content-desc` - 无障碍描述（用于图标按钮）

**点击坐标计算**：取 bounds 的中心点
- 中心 X = (x1 + x2) / 2
- 中心 Y = (y1 + y2) / 2

#### ADB 常用命令

```bash
# 查看设备列表
adb devices -l

# 截图
adb exec-out screencap -p > screenshot.png

# 模拟点击
adb shell input tap <x> <y>

# 模拟输入文字
adb shell input text "hello"

# 查看日志
adb logcat | grep -E "aurora|wave"

# 强制停止应用
adb shell am force-stop com.aurora.wave.dev
```

---

## 📝 修改日志

| 日期 | 版本 | 变更内容 |
|------|------|----------|
| 2025-12-10 | 0.1.3 | 完整实现语言切换功能（中英文即时切换）、移除消息页 FAB |
| 2025-12-10 | 0.1.2 | 重构页面架构移除内部Scaffold、添加 UI Automator 自动化测试流程 |
| 2025-12-10 | 0.1.1 | 新增登录/注册页面（动态波浪背景），UserDataStore，退出登录功能，隐藏部分菜单项 |
| 2025-12-09 | 0.1.0 | 初始架构搭建，UI 骨架完成 |

---

## 📞 联系方式

- **项目负责人**：[待填写]
- **技术负责人**：[待填写]
- **设计负责人**：[待填写]

---

> 本文档由 GitHub Copilot 辅助生成，如有疑问请联系项目组。
